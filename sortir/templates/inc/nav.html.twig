<div class="d-flex">
    <ul class="navbar-nav me-auto mb-2 mb-lg-0 me-2">

        {% set currentPath = path(app.request.attributes.get('_route'),
            app.request.attributes.get('_route_params')) %}

        {# TODO décommenter la condition pour que l'entete n'apparaisse pas si on est sur la page de login une fois les pages mises en place #}
        {# {% if 'login' not in currentPath %} #}
        <li class="nav-item"><a class="nav-link px-3" href="{{ path('ville_lister') }}" title="Villes">Villes</a></li>
        {# TODO: ajouter les liens vers le controlleur campus #}
        <li class="nav-item"><a class="nav-link px-3" href="#" title="Campus">Campus</a></li>


        <li class="nav-item"><a class="nav-link px-3" href="{{ path('accueil_accueil') }}" title="Accueil">Accueil</a></li>
        <li class="nav-item"><a class="nav-link px-3" href="{{ path('profil_profil') }}" title="Mon profil">Mon profil</a></li>
        <li class="nav-item"><a class="nav-link px-3" href="{{ path('app_logout') }}" title="Se deconnecter">Se déconnecter</a></li>
        {# {% endif %} #}


    </ul>
</div>