{% extends "base.html.twig" %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('build/form.css') }}" rel="stylesheet"/>
{% endblock %}

{% block title %}
    Register
{% endblock %}

{% block body %}
    <div class="regisForm-row">
        <div class="regisForm-col">
            <div>
            {{ form(registrationForm) }}
            </div>
        </div>
        <div class="regisForm-col">
            <div>
            {{ form(registrationCsv) }}
            </div>
        </div>
    </div>
{% endblock %}
